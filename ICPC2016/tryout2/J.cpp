#include <bits/stdc++.h>

using namespace std;

int main() {
    int N;
    scanf("%d ", &N);
    while (N--) {
        string line;
        getline(cin, line);
        cout << line.size() << "\n";
    }
}
